<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/tvTitleResults"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#31B404"
        android:gravity="center"
        android:padding="8dp"
        android:text="Test Results"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/tvResults"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/llBottomBar"
        android:layout_below="@+id/tvTitleResults"
        android:gravity="center"
        android:padding="8dp"
        android:text="No results found / Please select scan"
        android:textColor="#084B8A"
        android:textSize="16sp" />

    <LinearLayout
        android:id="@+id/llBottomBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#F2F5A9"
        android:padding="10dp" >

        <Button
            android:id="@+id/btnScan"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Test Scan QR Code" />
    </LinearLayout>

</RelativeLayout>